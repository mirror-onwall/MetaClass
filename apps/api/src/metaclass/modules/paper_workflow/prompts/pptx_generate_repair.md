# Stage 4：官方 pptx Skill 一次性轻量修复

遵循 `pptx` Skill 的修复与复验流程，只修复上一轮输出中的 JSON、speaker notes、轻微标题映射或版面 QA 问题。不得改变页面数量、顺序、主要论点、claim、source ref、asset id 或论文数字。

如果 presentation.pptx 已损坏、严重缺页或缺少关键图片，不要重建整份内容；返回 blocked，让当前 Stage 失败。
